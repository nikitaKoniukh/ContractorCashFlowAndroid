package com.yetzira.ContractorCashFlowAndroid.ui.theme

import androidx.compose.ui.unit.dp

object KablanProSpacing {
    val xs = 6.dp      // List row vertical padding
    val sm = 10.dp     // Badge horizontal, pills row spacing
    val md = 12.dp     // Stat pill horizontal, header inner spacing
    val lg = 14.dp     // KPI cards row spacing, row icon-to-text
    val xl = 18.dp     // Screen horizontal padding, card inner, VStack card spacing
    val xxl = 22.dp    // Screen vertical padding, analytics cards spacing
    val xxxl = 28.dp   // Card vertical padding, paywall sections spacing
    val financialColumnsSpacing = 36.dp  // Financial columns spacing
}
