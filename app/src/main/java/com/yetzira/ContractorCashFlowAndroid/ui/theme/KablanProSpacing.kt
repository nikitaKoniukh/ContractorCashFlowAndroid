package com.yetzira.ContractorCashFlowAndroid.ui.theme

import androidx.compose.ui.unit.dp

object KablanProSpacing {
    val xs = 4.dp      // List row vertical padding
    val sm = 8.dp      // Badge horizontal, pills row spacing
    val md = 10.dp     // Stat pill horizontal, header inner spacing
    val lg = 12.dp     // KPI cards row spacing, row icon-to-text
    val xl = 16.dp     // Screen horizontal padding, card inner, VStack card spacing
    val xxl = 20.dp    // Screen vertical padding, analytics cards spacing
    val xxxl = 24.dp   // Card vertical padding, paywall sections spacing
    val financialColumnsSpacing = 32.dp  // Financial columns spacing
}

